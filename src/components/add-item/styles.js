import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputContainer: {
        flexDirection: "row",
        marginHorizontal: 10,
        marginTop: 50,
    },
    input: {
        flex: 1,
        color: "#fff",
        backgroundColor: "#aaa",
        marginRight: 10,
        borderRadius: 5
    }
})